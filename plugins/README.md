# Plugins

Each subdirectory under `plugins/` is intended to be a standalone Copilot CLI plugin.

Only directories with a valid `plugin.json` are installable plugins. Placeholder directories are present so the repository can grow into a multi-SDK marketplace without another layout change later.
